# Postgres
POSTGRES_USER=dlt_user
POSTGRES_PASSWORD=change-me
POSTGRES_DB=dlt_db
POSTGRES_PORT=5434

# Webapp / API
WEB_HOST=0.0.0.0
WEB_PORT=8080
FRONTEND_ORIGIN=http://localhost:8080
DATABASE_URL=postgresql://dlt_user:change-me@postgres:5432/dlt_db
CLI_BASE=python -m webapp.backend.cli_runner
WEBAPP_API_KEY=change-me
DATAPIPELINES_API_KEY=change-me

# Scheduler toggle (true/false)
SCHEDULER_ENABLED=true

# Optional logging
LOG_LEVEL=INFO

# AWS SSM tunnel (optional)
AWS_REGION=eu-west-1
AWS_PROFILE=default
SSM_TARGET=
SSM_HOST=
SSM_REMOTE_PORT=5432
SSM_LOCAL_PORT=5433
SSM_EXPOSE_PORT=5432
